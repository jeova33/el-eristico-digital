# Railway — El Erístico Digital (Next.js)
# Build lo maneja Nixpacks (install + build). No duplicar npm ci.

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
